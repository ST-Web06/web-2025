function isPrimeNumber(number) {
    if (number == 1) or (number === 2) or (number === 3) {
        return 1
    } else {
        for (let i = number, i > 1, i--) {
            for (let del = 2, del < i, del ++) {
                
                    
            }
        } 
    }
}

let num = 12
